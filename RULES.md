# 기본 작업 규칙

작성일: 2026-08-13
최종 수정: 2026-08-13

이 문서는 본 프로젝트에서 Claude Code가 작업할 때 항상 준수해야 하는 기본 규칙을 기록합니다.

## 1. 언어
- 모든 설명과 답변은 한글로 작성한다.
- 중간 연산(계산 과정, 로직 추론 등) 과정도 한글로 서술한다.

## 2. 불확실성 고지
- 확실하지 않은 내용은 "확실하지 않다"고 명시적으로 밝힌다.
- 추측을 사실처럼 단정해서 말하지 않는다.

## 3. 수정 전 검토
- 분석이 완료된 후 코드/설정 등을 수정해야 할 경우, 수정 전에 다음을 반드시 검토하고 사용자에게 설명한다.
  - 수정이 필요한 이유
  - 수정으로 인한 영향도(사이드 이펙트, 리스크 등)

## 4. 세션 시작 시 원격 제어(Remote Control) 활성화
- `/remote-control`은 Claude가 `Skill` 도구로 호출하는 스킬이 아니라, VSCode 확장 자체의 내장 슬래시 명령/설정이다. 따라서 Claude가 매 세션마다 이를 "실행"할 수는 없다.
- 대신 VSCode 확장 Settings의 **"Enable Remote Control for all sessions"** 토글을 사용자가 켜두면, 모든 세션에서 상시 활성화된 상태로 유지되어 사실상 이 규칙이 충족된다.
- **[상태: 이행됨 - 사용자 설정]** 2026-08-13 기준, 해당 토글이 켜져 있음을 스크린샷으로 확인함. 추후 꺼짐이 확인되면 사용자에게 재활성화를 안내한다.

## 5. 변경 이력 기록
- 모든 진행사항 및 변경사항은 마크다운 파일로 기록한다.

## 6. 크로스 플랫폼 스크립트
- 작성하는 모든 스크립트는 Windows / macOS / Linux 세 환경에서 모두 동작하도록 작성한다.
- (예: 경로 구분자, 줄바꿈, 셸 종속 문법 등에 주의)

## 7. GitHub 저장소 동기화
- 이 프로젝트는 GitHub 저장소(`raon-servicetech2-docker-infra`, private)에 등록되어 관리된다.
- **WORKLOG.md만 변경되고 다른 실질적인 변경사항이 없는 경우에는 커밋/푸시하지 않는다.** WORKLOG.md 자체는 저장소에 포함(추적)하되, 로그 갱신만 있는 시점에는 미루고, 스크립트/문서/설정 등 실질적인 변경이 생겼을 때 WORKLOG.md 갱신분과 함께 커밋한다.
- 실질적인 업데이트가 있으면, **매번 사용자에게 묻지 않고 자동으로 commit + push한다.** (이 규칙 자체가 지속적 승인으로 간주됨)
- 비밀번호/토큰 등 민감정보는 어떤 파일에도 기록하지 않는다는 기존 원칙(대화 중 확립)을 유지하며, 커밋 전 민감정보 포함 여부를 항상 인지한다.

---

## 변경 이력

| 날짜 | 내용 |
|------|------|
| 2026-08-13 | 기본 규칙 1~6 최초 작성. 4번 규칙(`/remote-control`)은 해당 스킬 미확인으로 보류 처리. |
| 2026-08-13 | 4번 규칙 정정: `/remote-control`은 Skill 도구용 스킬이 아니라 VSCode 확장의 내장 명령/설정임을 확인. 사용자가 "Enable Remote Control for all sessions" 토글을 켜둔 것을 확인하여 이행 완료 상태로 변경. |
| 2026-08-13 | 7번 규칙(GitHub 저장소 동기화) 추가. 저장소명 `raon-servicetech2-docker-infra`(private) 확정. WORKLOG.md만 바뀐 경우는 커밋 제외, 실질 변경 시 자동 push로 확정. |
